# 部落格整理待辦

2026-09-22 跟網頁 Claude 討論後列的。做完一項就打勾、寫日期；細節與理由在 `web_開發記錄v0.1.md` 同一天那一節。

- [ ] **1. 工具鏈修好**：`eslint.config.js` 拿掉 `import/no-restricted-paths`、`import/no-cycle` 兩條死規則讓 `pnpm lint` 能跑；加 `vue-tsc --noEmit` 當 `pnpm typecheck`；CI 跑 lint、typecheck、build。
- [ ] **2. 分支收斂**：把 `develop_galaxy_tags` merge 進 `main`（`develop` 已包含 `main` 全部內容，反向差異只有 `resource/` 舊檔），之後只維護一支，不再 cherry-pick。**會改線上網站，使用者說了才做。**
- [ ] **3. 清死碼與雜物**：`svgHudPanel_back.vue`、`svgHudPanel_back2.vue`、`costomHudPanel.vue`、`customLayout.vue`、根目錄四個 build log、`resource/` 舊稿、`api-examples.md`、`markdown-theme-preview.md`、`tsconfig.json` 與 `config.mts` 裡不存在的 `widgets/`、`entities/`、沒裝 zod 卻存在的 `shared/utils/zod.ts`。`flowscker/` 搬出 `docs/pages/`。
- [ ] **4. 元件檔名統一 PascalCase**：`postCard.vue`、`dateBadge.vue` 這類 `git mv`；順手把 `costom` 改對。
- [ ] **5. 規範文章整理成「這個專案真的在用的版本」**：`zod-schema-型別使用規範.md` 收成一個版本、修筆誤（`z.iutput`、`SearchCategoryFormSchema`、`Servie`）、拿掉貼上的對話、加「這個專案哪些不採用與為什麼」，設為公開；`CLAUDE.md` 與 `web-code-style` skill 改成指向它，規範只留一份。**需要使用者參與。**
- [ ] **6. 叮咚後台要不要導入 Zod Parser**：唯一有 API 的地方，可以當規範的實戰範例。先決定型別留 snake_case 對照 `api.md`，還是轉 camelCase 照規範，兩者選一。**先討論再動。**
- [ ] **7. 套件與框架升版**：見 2026-09-22 的更新建議表（開發記錄）。
