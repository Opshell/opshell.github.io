# 部落格整理：執行順序

2026-09-22 跟網頁 Claude 討論後定的。原則：**先合分支、再做減法、再修工具、再升版、最後改命名與規範**。做完一項打勾、寫日期與 commit；理由與細節在 `web_開發記錄v0.1.md`。

## 第 1 步：分支收斂（一次做完，之後只有 `main`）

- [ ] 導覽列「3D Galaxy Posts」加「beta」字樣
- [ ] `develop_galaxy_tags` merge 進 `main`、push、確認 Actions 綠、線上 `/galaxy-posts.html` 與 `/dindon/dashboard/` 都正常
- [ ] 之後所有工作直接在 `main`；`develop_galaxy_tags` 留著不動或刪掉

> 星系頁以 beta 上線：功能都驗過，剩 SVG 面板定稿與備份檔清理，屬打磨。它是延遲載入的獨立頁，不影響其他頁。

## 第 2 步：減法（兩個 commit）

- [ ] 移除沒用到的套件：`vitest`、`@vitest/ui`、`jsdom`、`@vue/test-utils`、`@pinia/testing`、`markdown-it-footnote`、`@mdit/plugin-tasklist`；`config.mts` 的 `test:` 區塊一起拿掉
- [ ] 清死碼與雜物：`svgHudPanel_back.vue`、`svgHudPanel_back2.vue`、`costomHudPanel.vue`、`customLayout.vue`、根目錄四個 build log、`resource/` 舊稿、`api-examples.md`、`markdown-theme-preview.md`、`tsconfig.json` 與 `config.mts` 裡不存在的 `widgets/`、`entities/`、沒裝 zod 的 `shared/utils/zod.ts`；`flowscker/` 搬出 `docs/pages/`
  - 設計系統頁的 `components.vue` 目前拿三個 HUD 版本並排比較，刪備份前先決定要不要留那個比較

## 第 3 步：工具鏈（原待辦 1 ＋ 升版第 2 波，一起做）

- [ ] `eslint` 9.39 → 10、`@antfu/eslint-config` 6 → 9；重寫 `eslint.config.js`（刪 `import/no-restricted-paths`、`import/no-cycle`，stylistic 設定重新對一遍），`pnpm lint` 要能跑完整個倉庫
- [ ] `stylelint` 16 → 17、`stylelint-config-standard-scss` 17、`stylelint-config-standard-vue` 2、`stylelint-order` 8、`postcss-html` 2；主題 SCSS 那 57 個既有錯誤趁這次一次修
- [ ] `unplugin-auto-import` → 21、`unplugin-vue-components` → 32；重生 `docs/types/*.d.ts`
- [ ] 加 `vue-tsc`，`pnpm typecheck`；TypeScript 留 5.9（或升 6.x），7.0 等 vue-tsc 明確支援
- [ ] CI 加 lint、typecheck，build 之前跑

## 第 4 步：執行期套件升版（升版第 1 波）

- [ ] `vitepress` 1.3.2 → 1.6.4（掃 1.4～1.6 changelog）、`vue` → 3.5.x 最新
- [ ] `three`／`@types/three` → 0.186、`@tresjs/core`、`cientos` → 5.9、`post-processing` → 3.8、`postprocessing` → 6.39；星系頁實測
- [ ] `sass`、`gsap`、`axios`、`globby`、`sharp`、`postcss`、`@vueuse/core` 15、`@types/markdown-it-container` 4
- [ ] （可選）`pnpm` 10.28 → 12，`packageManager` 與 CI 一起改

## 第 5 步：命名統一

- [ ] `.vue` 檔名全部 PascalCase（`postCard.vue`、`dateBadge.vue`、`heatmap.vue`、`lightbox.vue`…），`git mv`，`index.ts` 與引用一起改；`costom` 改對
- [ ] `CLAUDE.md`、`web-code-style` skill 同步改成「一律 PascalCase」

## 第 6 步：規範只留一份（需要使用者）

- [ ] `zod-schema-型別使用規範.md` 收成一個版本：修筆誤（`z.iutput`、`SearchCategoryFormSchema`、`Servie`）、拿掉貼上的對話、SFC 結構三段合一、補 parse 失敗處理與 query key 歸屬、加「這個專案哪些不採用與為什麼」
- [ ] 設為公開；`CLAUDE.md` 與 `web-code-style` skill 改成指向它

## 第 7 步：叮咚後台要不要導入 Zod（先討論）

- [ ] 決定型別留 snake_case 對照 `api.md`，還是轉 camelCase 照規範
- [ ] 決定了才動 `features/dindon/dashboard/api.ts`

## 等外部

- [ ] VitePress 2.0 stable（Vite 8）；到時處理停更的 `vite-plugin-svg-icons`、`vitepress-plugin-sandpack`
- [ ] TypeScript 7 等 vue-tsc、typescript-eslint 支援
- [ ] Node 26 轉 LTS（2026-10）再考慮
